//#define XERR
#include "cell.ih"

bool cell::getStart() const
{
    //if (d_start == true)
    //    cout << "Start node with " << "width of " << d_x <<  " and height of " << d_y << endl;
    return d_start;
}
